<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fsb.e_commerceapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/mahdi"
        android:label="@string/app_name"
        android:roundIcon="@drawable/mahdi"
        android:supportsRtl="true"
        android:theme="@style/Theme.ECommerceApplication">
        <activity
            android:name=".activities.SettingsActivity"
            android:exported="true" />
        <activity
            android:name=".activities.SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.PlacedOrderActivity"
            android:exported="true"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".activities.NavCategoryActivity"
            android:exported="true"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".activities.DetailedActivity"
            android:exported="true"
            android:parentActivityName=".activities.NavCategoryActivity" />
        <activity
            android:name=".activities.ViewAllActivity"
            android:exported="true"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".activities.HomeActivity"
            android:exported="true"></activity>
        <activity
            android:name=".activities.RegistrationActivity"
            android:exported="true" />
        <activity
            android:name=".activities.loginActivity"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ECommerceApplication.NoActionBar" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>